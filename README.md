# ENFlow normalizing flow model

Installation:

    python3 -m venv venv
    . venv/bin/activate
    # run the correct pytorch install command for your accelerator
    # https://pytorch.org/get-started/
    # e.g. pip3 install torch torchvision torchaudio
    pip install -r requirements.txt

Running:


